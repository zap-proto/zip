[package]
name = "zip"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "A ZAP-native web framework: one registration, every projection."

[dependencies]
zip-macros = { path = "../zip-macros" }
